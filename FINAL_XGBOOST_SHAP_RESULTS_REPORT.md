# Final XGBoost + SHAP Results Report

Colab 실행 링크:

https://colab.research.google.com/github/hyeon03-sketch/IML-Final-project/blob/codex/ml-project-review/notebooks/IML_Final_Project_Final_XGBoost_SHAP_Colab.ipynb

## 1. 핵심 결론

최종 실험에서는 XGBoost 하나만 사용해 feature set을 비교했다. 가장 좋은 예측 성능은 `B_spatial_no_dong`에서 나왔다.

| 항목 | 결과 |
|---|---|
| Best feature set | `B_spatial_no_dong` |
| Test RMSE | 2,204.22만원 |
| Test MAE | 1,505.42만원 |
| Test MAPE | 11.19% |
| Test R² | 0.8981 |

결론은 명확하다.

- 공간파생변수를 추가하면 baseline보다 성능이 좋아진다.
- 다만 가장 중요한 예측 요인은 공간변수가 아니라 `건물연령`, `전용면적`, `층` 같은 주택 구조 변수다.
- 공간변수는 핵심 주역이라기보다, 구조 변수 중심 모델을 보완하는 지역 환경 정보로 해석하는 것이 타당하다.
- `읍면동`을 포함하면 공간변수의 추가 효과는 작아진다. 이는 공간변수와 행정동 정보가 일부 겹치기 때문이다.

## 2. 데이터와 Target

| 항목 | 값 |
|---|---:|
| 전체 거래 | 4,470건 |
| 전세 거래 | 2,486건 |
| 월세 거래 | 1,984건 |
| 읍면동 수 | 26개 |
| Target | `전세환산보증금(만원)` |
| 전월세전환율 | 6.5% |

월세 거래는 제거하지 않고 전세환산보증금으로 변환했다.

```text
전세환산보증금 = 보증금(만원) + 월세금(만원) * 12 / 0.065
```

데이터 정합성 점검 결과, 같은 `읍면동` 안에서 `동_초등학교수`, `동_총학교수` 값이 일부 달랐다. 동 단위 변수라는 정의에 맞게 동별 최빈값으로 통일했다.

| 변수 | 불일치 동 수 | 수정 행 수 |
|---|---:|---:|
| `동_초등학교수` | 8 | 79 |
| `동_총학교수` | 8 | 79 |

## 3. Feature Set별 성능

| Feature set | 의미 | RMSE | MAE | MAPE | R² |
|---|---|---:|---:|---:|---:|
| `A_baseline_no_dong` | 기본 변수 | 2,308.27 | 1,603.66 | 12.00% | 0.8883 |
| `B_spatial_no_dong` | 기본 + 공간변수 | 2,204.22 | 1,505.42 | 11.19% | 0.8981 |
| `A_location_baseline` | 기본 + 읍면동 | 2,216.95 | 1,518.12 | 11.40% | 0.8969 |
| `B_location_spatial` | 기본 + 읍면동 + 공간변수 | 2,208.91 | 1,515.95 | 11.39% | 0.8977 |

가장 좋은 모델은 `B_spatial_no_dong`이다. 즉, 읍면동을 직접 넣는 것보다 동 단위 공간파생변수를 넣은 모델이 가장 낮은 RMSE를 보였다.

## 4. 공간변수 추가 효과

| 비교 | ΔRMSE | ΔMAE | ΔMAPE | ΔR² | 해석 |
|---|---:|---:|---:|---:|---|
| `B_spatial_no_dong - A_baseline_no_dong` | -104.06 | -98.24 | -0.81%p | +0.0098 | 공간변수 추가 효과 뚜렷 |
| `B_location_spatial - A_location_baseline` | -8.04 | -2.17 | -0.01%p | +0.0007 | 읍면동 통제 후에는 효과 작음 |

따라서 공간변수의 효과는 있다. 다만 `읍면동`을 넣으면 그 효과가 크게 줄어든다.

## 5. SHAP 해석

### Best model: `B_spatial_no_dong`

| 변수군 | SHAP 비중 |
|---|---:|
| 주택 구조 변수 | 78.50% |
| 학교 변수 | 13.90% |
| 공원 변수 | 4.69% |
| 계약 시점 변수 | 2.26% |
| 바다 변수 | 0.65% |

공간변수 전체 비중은 `학교 + 공원 + 바다 = 19.24%`이다.

상위 변수는 다음 순서다.

| 순위 | 변수 |
|---:|---|
| 1 | `건물연령(계약기준)` |
| 2 | `전용면적(㎡)` |
| 3 | `동_초등학교수` |
| 4 | `층` |
| 5 | `공원수` |

### 읍면동 통제 모델: `B_location_spatial`

| 변수군 | SHAP 비중 |
|---|---:|
| 주택 구조 변수 | 75.51% |
| 학교 변수 | 11.80% |
| 읍면동 위치정보 | 5.39% |
| 공원 변수 | 5.03% |
| 계약 시점 변수 | 1.86% |
| 바다 변수 | 0.42% |

읍면동을 포함해도 공간변수 전체 비중은 17.24%로 남아 있다. 따라서 공간변수는 완전히 사라지는 정보가 아니라, 읍면동 위치정보와 겹치면서도 일부 독립적인 설명력을 갖는다.

## 6. Strict Validation

Random split만으로 성능을 주장하면 과대평가 위험이 있다. 그래서 시간 기준, 단지 기준, 동 기준 holdout을 추가했다.

| 비교 | Random | Time holdout | Complex holdout | Dong holdout |
|---|---|---|---|---|
| `B_spatial_no_dong - A_baseline_no_dong` | 개선 | 개선 | 개선 | RMSE/R²는 약화, MAE/MAPE는 개선 |
| `B_location_spatial - A_location_baseline` | 개선 | 개선 | 개선 | 개선 |

핵심은 `B_location_spatial - A_location_baseline`이다. 읍면동을 이미 넣은 보수적 조건에서도 모든 strict validation에서 공간변수 추가 후 성능이 개선됐다.

## 7. 최종 보고서용 문장

> XGBoost 기반 전세환산보증금 예측에서 공간파생변수를 추가한 `B_spatial_no_dong` 모델이 가장 좋은 성능을 보였으며, Test RMSE 2,204만원, MAE 1,505만원, MAPE 11.19%, R² 0.8981을 기록했다. SHAP 분석 결과 예측의 핵심은 건물연령, 전용면적, 층 등 주택 구조 변수였지만, 학교·공원·바다로 구성된 공간변수도 전체 기여도의 19.24%를 차지했다. 읍면동을 통제한 모델에서도 공간변수의 SHAP 비중은 17.24%로 유지되었고, strict validation에서도 대체로 성능 개선 방향이 유지되었다. 따라서 공간파생변수는 전세가격 예측의 주된 요인이라기보다는, 주택 구조 변수 중심의 XGBoost 모델을 보완하는 의미 있는 지역 환경 정보로 해석하는 것이 타당하다.

## 8. 발표용 한 문장

> 최종 XGBoost 실험에서 공간파생변수 추가 모델은 RMSE 2,204만원, MAPE 11.19%, R² 0.8981로 가장 좋은 성능을 보였고, SHAP 기준 공간변수는 전체 예측 기여도의 약 19%를 차지해 구조 변수 중심 모델을 보완하는 정보로 확인되었다.
